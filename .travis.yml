sudo: false
addons:
  apt_packages:
    - libgl1-mesa-dev
    - xorg-dev
language: go
go:
  - 1.x
  - master
env:
  - GO111MODULE=on
matrix:
  allow_failures:
    - go: master
  fast_finish: true
script:
  - diff -u <(echo -n) <(gofmt -d -s .)
  - go vet ./...
  - go test -v -race ./...
